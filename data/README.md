# Data Directory

Generated data is stored here locally and is gitignored:

- `raw/`: downloaded SEC filings and price CSVs
- `interim/`: normalized event and price rows
- `processed/`: feature bundles
- `runs/`: checkpoints, metrics, tables, and figures

Do not commit downloaded filings, price histories, checkpoints, or generated results.
