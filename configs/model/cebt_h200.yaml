model:
  price_features: 8
  metadata_features: 6
  event_embedding_dim: 512
  hidden_dim: 192
  latent_dim: 16
  output_dim: 3
  dropout: 0.15
loss:
  supervised_weight: 1.0
  control_delta_weight: 0.35
  kl_weight: 0.01
  sparsity_weight: 0.001
  consistency_weight: 0.05
